/* ----------------------------------------------------------------------- */
/* Template source file generated by piklab */
#include <pic16f877.h>
#include "macros.h"

/* ----------------------------------------------------------------------- */
/* Configuration bits: adapt to your setup and needs */
typedef unsigned int word;
word at 0x2007 CONFIG = _RC_OSC & _WDT_ON & _PWRTE_OFF & _BODEN_ON & _LVP_ON & _CPD_OFF & _WRT_ENABLE_ON & _DEBUG_OFF & _CP_OFF;

void main() {
    /* << insert code >> */
    unsigned int i = 0;
 
  BIT_CLEAR(OPTION_REG, 7);
  BIT_SET(TRISB, 0);  	//B0 as input
  BIT_CLEAR(TRISB,1); 	//B1 as output
  BIT_CLEAR(PORTB,1); 	//turn off B1
  while(1){
    if(BIT_TEST(PORTB,0) == 1) 
	BIT_CLEAR(PORTB, 1);
    else
	BIT_SET(PORTB,1); 
  }
}

